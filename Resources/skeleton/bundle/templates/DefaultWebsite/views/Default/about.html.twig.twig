{{ '{% extends "' }}{{ bundle }}{{ '::layout.html.twig" %}' }}
{{ '{% block content %}' }}
    <div class="page-header">
        <h1>About</h1>
    </div>
    <div class="editable">{{ '{{ editor_get_content("' }}{{ bundle }}-{{ controller }}-{{ action.basename }}{{ '", "Website description") }}' }}</div>
{{ '{% endblock %}' }}
